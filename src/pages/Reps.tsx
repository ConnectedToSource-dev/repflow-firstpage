export default function Reps() {
  return (
    <section className="section" style={{ paddingTop: 'calc(var(--nav-height) + var(--space-16))' }}>
      <div className="container">
        <span className="eyebrow">For Sales Teams</span>
        <h1 className="heading-display xl" style={{ marginTop: 'var(--space-4)' }}>
          Coming soon
        </h1>
        <p className="body-text" style={{ marginTop: 'var(--space-4)' }}>
          The sales team funnel page is under construction.
        </p>
      </div>
    </section>
  )
}
